// Mock for FacebookAdsService
export const FacebookAdsService = {
  authenticate: jest.fn().mockResolvedValue(true),
};

// Mock for GoogleAdsService  
export const GoogleAdsService = {
  authenticate: jest.fn().mockResolvedValue(true),
};

// Mock for GoHighLevelService
export const GoHighLevelService = {
  authenticate: jest.fn().mockResolvedValue(true),
};
