{
  "query": "SELECT segments.date, metrics.conversions, metrics.cost_micros, metrics.impressions, metrics.clicks FROM customer WHERE segments.date BETWEEN '2024-01-01' AND '2024-12-31' ORDER BY segments.date DESC LIMIT 10"
}
